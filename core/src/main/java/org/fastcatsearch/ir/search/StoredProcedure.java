package org.fastcatsearch.ir.search;


/*
 * 미리정해놓은 로직에 따라 검색되도록 만드는 프로시져.
 * Query를 상속받아서 동일한 인터페이스제공.
 * 단순히 쿼리를 modify하는 것으로는 구현이 어려운 내부 객체핸들링이 필요할때 여기서 구현을 해버린다.
 * */
public abstract class StoredProcedure {
	
}
